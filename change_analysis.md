# FreeNet 初步修改分析

> 文档性质：需求与初步方案分析  
> 当前阶段：方案确认前  
> 分析对象：仓库中的 FreeNet 官方实现  
> 目标：使用 FreeNet 模型训练当前高光谱遥感数据集

本文只用于明确需求、实施边界和初步改动方向，不作为具体实施任务清单。涉及文件、接口、参数、测试和执行顺序的详细任务，应在方案逐项确认后另建文档。

# 第一章 总体需求与修改方向

## 1. 模型使用方式

### 1.1 现有功能和实现

官方实现提供了完整的 FreeNet 网络。模型由多阶段特征提取、SE 模块、逐级下采样和多尺度特征融合组成，最终输出像素级分类结果。

当前模型与 SimpleCV 框架绑定。模型初始化依赖框架配置和注册机制，训练时在模型内部计算损失，推理时在模型内部输出分类概率。

### 1.2 后续需求和实施边界

后续需要使用 FreeNet 的核心网络结构处理当前数据集的 224 波段影像，并输出当前任务所需的 31 个已知类别。

实施边界如下：

- 保留 FreeNet 的核心结构和主要设计思想；
- 不把模型替换成其他网络；
- 不直接破坏官方实现，官方代码继续作为参照；
- 可以调整模型的输入、输出和框架接口；
- 第一阶段不主动改造网络结构或增加新的复杂模块；
- 是否继续使用 SimpleCV，需要在环境和维护成本分析后决定。

### 1.3 总体改动方向

需要让 FreeNet 的输入通道数和输出类别数适配当前任务，并解除或封装与原训练框架不兼容的接口。网络内部结构是否需要进一步调整，应在基础版本能够正常训练后再判断。

## 2. 数据读入方式

### 2.1 现有功能和实现

官方数据代码主要面向 Pavia、Salinas 和 GRSS2013 等经典数据集。这些数据通常以一幅完整高光谱影像和一幅标签图的形式读入。

官方实现会在同一幅影像中划分训练和测试像素，并通过像素指示图控制每次参与损失计算的样本。其 DataLoader 实际上反复返回完整影像，而不是读取多幅独立瓦片。

### 2.2 后续需求和实施边界

当前数据由多幅独立 TIF 瓦片组成。每幅影像为 224 波段，标签中的 0 表示未标注区域，1 至 31 表示已知类别。训练影像分布在多个目录中，并通过文件名与标签配对。

后续数据模块需要实现：

- 扫描和匹配多目录中的影像与标签；
- 正确读取多波段 TIF；
- 处理未标注像元和无数据黑边；
- 为训练、验证和推理提供统一的数据格式；
- 支持可复现的数据划分。

实施边界如下：

- 不改变原始数据文件和标签定义；
- 不在影像不完整时开始正式训练；
- 训练集和验证集之间应尽量避免同一场景的信息泄漏；
- 数据增强、裁剪和采样方法尚未确定，需要后续单独比较；
- 暂不把数据预处理结果永久写回原始数据目录。

### 2.3 总体改动方向

官方面向单幅整景的数据层不能直接用于当前任务，需要建立面向多瓦片数据的新数据入口。现有官方数据代码可以保留作为参考，但不宜在其现有抽象上继续堆叠当前数据逻辑。

## 3. 训练方法

### 3.1 现有功能和实现

官方训练方法基于 SimpleCV。它在完整影像上进行前向计算，但每次只选择一部分像素参与损失，以此实现像素级小批次训练。

官方示例使用 SGD、poly 学习率和固定迭代次数，并由 SimpleCV 负责配置、日志、评估和 checkpoint 管理。

### 3.2 后续需求和实施边界

后续需要形成适用于多瓦片数据的完整训练流程，包括数据批次、损失计算、优化、验证、模型保存和断点恢复。

实施边界如下：

- 标签 0 不参与监督损失；
- 训练过程需要适应类别分布不均衡；
- 训练方法必须支持当前可用的计算设备；
- 第一阶段以得到可靠的 FreeNet 闭集分类基线为目标；
- 优化器、学习率、类别平衡方法和训练尺寸暂不在本阶段定案；
- 不在基础训练链路验证前直接开展长时间正式训练。

### 3.3 总体改动方向

需要把官方的“单幅影像加像素采样”训练方式调整为“多瓦片数据批次”训练方式，并建立独立、清晰且可维护的训练入口。是否保留官方优化策略，应通过后续对照实验决定。

## 4. 验证与模型选择

### 4.1 现有功能和实现

官方验证主要面向单幅测试影像，使用 OA、AA、Kappa 和逐类准确率评价模型。经典数据集的训练和测试像素可能来自同一幅整景。

### 4.2 后续需求和实施边界

当前任务更接近跨瓦片、跨场景的像素分类，需要建立能够反映泛化能力的验证方式。除总体准确率外，还需要关注类别不平衡条件下的 macro-F1 和逐类结果。

实施边界如下：

- 验证数据不能参与归一化统计和训练；
- 同一场景的相邻瓦片原则上不能同时出现在训练集和验证集；
- 数据中部分稀有类别只出现在极少场景，验证划分需要显式说明类别覆盖情况；
- 不使用不完整类别的单一指标掩盖验证集局限；
- 最终的数据划分和主指标需在实施前单独确认。

### 4.3 总体改动方向

需要从官方的同景像素验证转向场景级数据划分，并补充适合当前任务的评价指标。验证方案应先独立分析类别和场景分布，再决定具体划分。

## 5. 推理和任务输出

### 5.1 现有功能和实现

官方模型能够对完整影像输出各类别概率，官方代码主要用于数据集内部评估，没有覆盖当前任务的测试文件遍历、预测 TIF 保存和提交打包流程。

### 5.2 后续需求和实施边界

后续需要使用训练完成的 FreeNet 对测试瓦片逐张推理，恢复原始空间尺寸，并输出与测试文件一一对应的分类结果。

实施边界如下：

- 输出文件名、尺寸和数据类型应符合任务要求；
- 已知类预测和未知类处理应分开设计；
- 第一阶段先完成 31 个已知类的闭集推理；
- 未知类 ID、判断方法和阈值在规则确认与实验完成前不定案；
- TTA、滑窗和其他推理增强不属于基础版本的必需功能。

### 5.3 总体改动方向

需要在 FreeNet 训练流程之外增加面向当前数据集的推理和结果输出入口。开放集或未知类处理应作为闭集基线之后的独立问题研究。

## 6. 工程组织与兼容性

### 6.1 现有功能和实现

官方仓库将模型、数据、配置和 SimpleCV 训练入口组织在一起，主要服务于三个官方示例数据集。现有仓库中另有一套面向当前数据集的 UNet baseline。

### 6.2 后续需求和实施边界

后续实现需要能够与官方 FreeNet 和现有 baseline 并存，便于结构对照、结果比较和问题定位。

实施边界如下：

- 不覆盖官方示例和配置；
- 不把 FreeNet 适配逻辑混入 UNet 模型实现；
- 公共数据逻辑可以考虑复用，但依赖关系必须清楚；
- 新实现应记录运行配置和数据划分，保证实验可复现；
- 具体目录和文件组织在方案确认后确定。

### 6.3 总体改动方向

宜将 FreeNet 适配实现与官方代码分开组织，并尽量减少对旧框架的隐式依赖。最终采用独立目录还是抽取公共模块，需要结合下一阶段的方案分析决定。

# 第二章 初步分析与方案构想

## 1. 模型适配方案构想

FreeNet 的主体是普通二维卷积网络，能够直接接收 224 波段作为输入通道，因此当前任务不要求改变其基本计算方式。最小适配范围主要是输入通道、输出类别和模型接口。

初步存在两种方案：

**方案 A：继续使用 SimpleCV。** 该方案更接近官方运行方式，但需要安装和维护较旧的框架依赖，还需要把当前数据接入其注册、配置和训练体系。优势是保留官方训练框架，劣势是环境兼容性和后期维护成本较高。

**方案 B：迁移为纯 PyTorch 模型。** 保留官方 FreeNet 的结构，将框架基类、注册器和框架内 SE 模块替换为普通 PyTorch 实现。优势是接口清楚，可直接接入当前数据和训练代码；风险是需要仔细核对迁移后的结构与官方实现是否一致。

目前更倾向方案 B，但应先确认是否存在必须复现官方框架行为的实验要求。基础版本应保持网络结构不变，结构改进放在完成基线之后。

## 2. 数据组织方案构想

当前数据天然以 512×512 瓦片为样本，适合建立按文件读取的 Dataset。每个样本可以返回一幅标准化影像和对应标签，标签 0 在进入损失前映射为忽略值。

数据归一化可沿用逐波段 mean/std 的思路，但统计量只能由训练数据计算，并需要保存以供验证和推理复用。无数据黑边是否参与统计、归一化后如何处理，应保持一致并通过样本检查确认。

训练输入初步有两种方式：

- 完整瓦片训练：更符合 FreeNet 的 patch-free 特点，空间上下文完整，但显存消耗较大；
- 裁剪训练：显存压力较小，可以产生更多随机样本，但可能削弱完整场景上下文，并引入裁剪采样偏差。

两种方式暂不提前定案。应先用模型显存测试确定完整瓦片是否可行，再决定是否需要裁剪和怎样裁剪。

## 3. 数据划分方案构想

按单张瓦片随机划分最简单，但同一航线或场景的相邻瓦片可能高度相似，容易造成验证分数虚高。因此，主验证方案应优先考虑按文件名中的场景标识分组。

当前类别分布对严格场景划分存在限制。类别 21、24、28 各自只出现在一个场景中；如果对应场景进入验证集，训练集将完全缺少这些类别。如果全部保留在训练集，验证集又无法评价这些类别。

初步思路是优先保证训练集覆盖全部类别，再在剩余场景中选择验证集，并在结果中明确验证集实际覆盖的类别。是否另外保留一个辅助的瓦片级验证集，需要在主验证方案确认后决定。

## 4. 训练方案构想

官方像素指示图采样主要用于单幅整景和少量标注像素的设置。当前数据已有多幅独立瓦片，可以直接对每个 batch 中所有已标注像元计算损失，不必机械复用官方 sampler。

基础损失可先考虑交叉熵并忽略标签 0。由于类别像素数量差异较大，可以比较普通交叉熵、类别加权或采样平衡，但不应在缺少对照实验时直接确定某一种方法。

优化策略可以同时保留两条思路：

- 使用官方 SGD 和 poly 学习率，观察是否能复现其训练特征；
- 使用当前语义分割中更常见的优化器和调度方式，作为工程基线。

第一步应先完成单批次前向、反向和短周期训练，确认数据、标签、损失和 checkpoint 链路正确，再讨论正式训练参数。

## 5. 验证方案构想

当前任务类别不平衡明显，只使用 OA 可能掩盖稀有类别表现。初步建议同时观察 OA、macro-F1 和逐类 F1，并保留 AA 或 Kappa 作为与官方实验口径的补充对照。

macro-F1 的计算应只对验证标签中实际出现的类别取平均，同时单独列出缺失类别。这样能够避免把不存在的类别错误计为 0 分，但也必须防止不同划分之间因参与类别不同而被直接比较。

主模型选择指标和最终 split 尚未确定，应在查看完整数据的场景—类别分布后单独确认。

## 6. 推理方案构想

基础推理可以按 512×512 整瓦片执行，模型输出 logits 后再统一计算概率和类别。若完整瓦片显存不足，再考虑滑窗推理。

测试时增强可以提高稳定性，但会增加推理时间，适合作为基础推理正确后的可选实验。预测 TIF 的文件名、尺寸、整数类型和标签范围需要与任务要求保持一致。

未知类不能仅凭闭集验证结果可靠确定。置信度阈值可以作为初步基线，但应在确认比赛规则和可用验证方式之后单独制定方案。

## 7. 下一阶段文档安排

本文件只保存需求分析和初步方案，不继续扩展为任务清单。

下一步应按本章主题逐项确认以下决策：

1. 是否采用纯 PyTorch 迁移；
2. 完整瓦片还是裁剪训练；
3. 场景级验证集如何构造；
4. 基础损失和首轮优化策略；
5. 闭集基线与未知类处理的阶段划分；
6. 新实现与现有 baseline 的代码组织关系。

待上述决策得到确认后，再另建“FreeNet 实施任务清单”，将已确定的方案拆分为具体文件修改、测试和执行任务。未确认的方案不应提前写入实施清单。
